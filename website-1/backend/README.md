# Backend

Express + MongoDB API for the Golden Response ecommerce platform.

## Setup

1. Copy `.env.example` to `.env` and fill values.
2. Install dependencies: `npm install`
3. Run dev server: `npm run dev`

## Environment Variables

See `.env.example`.
